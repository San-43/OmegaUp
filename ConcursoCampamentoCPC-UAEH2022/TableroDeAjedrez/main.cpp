#include <bits/stdc++.h>

using namespace std;
#define endl '\n'



int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    int q;

    cin >> n >> q;
    int list[n + 5];
    for (int i = 0; i < n; ++i)
        cin >> list[i];

    for (int i = 0; i < q; ++i) {
        int parImpar;
        cin >> parImpar;
    }

    return 0;
}
